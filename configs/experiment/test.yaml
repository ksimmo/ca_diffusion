# @package _global_
defaults:
  - override /data: neurofinder_image
  - override /model: unsupervised_ca_image_diffusion

tag: test

data:
  train:
    batch_size: 128
    num_workers: 16

model:
  model: 
    channels_in: 1
